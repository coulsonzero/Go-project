package main

import (
	"fmt"
)

func main() {
	// version2 := "1.1.2"
	// temp := "f"
	// arr := strings.Split(temp, ",")
	// fmt.Printf("%v \n", arr)
	fmt.Println(1 ^ 0)
	s := []byte("hello, world")
	s[2] = 'a'
	fmt.Println(string(s))

}
